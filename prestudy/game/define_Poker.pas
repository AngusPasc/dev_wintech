unit define_Poker;

interface

{ 54 уееф
  4 * 13 = 52 + 2

  54 * 4 = 216
}

//type
//  TPokerCard = ();
  
implementation

(*
  for i := 1 to 54 do
  begin
    tmpColor = (i + 3) div 4;
    tmpPoint = (i + 3) mod 4;
    Memo1.Lines.Add(IntToStr(tmpColor) + ' -- ' + IntToStr(tmpPoint));
  end;
*)
end.
